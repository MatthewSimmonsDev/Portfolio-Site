extends layout.pug

block content

  article.portfolio-intro

    h1 A view of my work

    p This is my portfolio that I created from sources such as The Odin Project and Team Treehouse


  article.portfolio-project-thumbs
    .projects-box.box.grid
      each project in data 
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`${project.image_urls[0]}`, alt=`thumbnail image `)
            h5= project.project_name

